[project]
name = "quasi"
version = "0.1.0"
description = "Quasi-analytical solver for nonlinear flux functions."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.5.0",
    "sympy>=1.14.0",
]
